server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://matchzone-db.c7suiaskuhog.us-east-2.rds.amazonaws.com:5432/postgres
    username: matchzone
    password: matchzone123456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # O "create" si quieres que borre y cree todo desde cero cada vez
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always # Para que ejecute data.sql o schema.sql si existen
  mail:
    host: smtp.gmail.com
    port: 587
    username: matchzoneapp@gmail.com
    password: gafvvovbtfbqcikq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
jwt:
  secret: tu_clave_secreta_super_segura_y_larga_para_jwt
  expiration: 86400000 # 1 día en milisegundos
  email-verification-expiration: 900000 # 15 minutos

# Nivel de logs
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    
app:
  email:
    from: matchzone@gmail.com
    verification-url: http://localhost:8080/auth/verify
